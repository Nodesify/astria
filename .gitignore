/target
**/*.rs.bk
node_modules/
packages/astria-cli/dist/
*.node
.claude/
.codex/
.cursor/
.gemini/
.kiro/
.graphify/
.astria/
.fastembed_cache/

# benchmark scratch workspace
bench-work/

# local tooling state (agent sessions, browser tests, per-tool agent config
# generated by `astria install` — AGENTS.md is the tracked cross-tool copy)
.mimosa/
.playwright-mcp/
.zcode/
.agents/
.opencode/
CLAUDE.md
GEMINI.md
docs/motion-docs.db

# benchmark scratch output (canonical results live in worked/)
scripts/bench/quality/out/

# worked-example full graphs (multi-MB, regenerable via the reproduce commands)
worked/astria/graph.json
worked/graphify-python/graph.json

# fetched URL documents (astria add <url>)
raw/

# local-only: OpenRouter judge key for promptfoo smoke runs (never commit)
.env.local
