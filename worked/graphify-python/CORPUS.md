# Corpus

The corpus for this worked example is the original Python graphify at
commit `91f4d12` of https://github.com/safishamsi/graphify (Apache-2.0/MIT) — 20
Python modules, tests, skill files, and docs, ~0.5 MB of text.

The source is intentionally **not vendored** here: it is third-party
executable code (URL fetchers, exporters) that does not belong in this
repository's tree. Clone it and run the example yourself:

```bash
git clone https://github.com/safishamsi/graphify corpus
cd corpus && git checkout 91f4d12
astria run . --embed --wiki        # current binary
# as run pre-1.0: nodesify-graphify run . --embed --wiki
```

All numbers in `review.md` were produced from that exact commit.

## Licensing and attribution

Graphify is © Graphify Labs and contributors, dual-licensed Apache-2.0/MIT.
The files in this directory (`graph_report.md`, `wiki_index.md`)
are outputs generated by running Graphify at the pinned commit on its own
repository; they are included here solely to make the benchmark numbers
verifiable. The full `graph.json` (2.4 MB) is regenerable with the command
above and is not stored in the repo. astria is an independent project — not
affiliated with, sponsored by, or endorsed by Graphify Labs.
