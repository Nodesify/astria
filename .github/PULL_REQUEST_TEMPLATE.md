## What and why

<!-- Describe the change and the motivation. Link related issues (`Fixes #123`). -->

## How tested

- [ ] `cargo test` passes
- [ ] `cd packages/astria-cli && npm test` passes (after `npm run build`)
- [ ] `cd website && npm run build` passes (if docs changed)
- [ ] User-facing behavior changes update `website/docs/` in the same PR

## Notes for reviewers

<!-- Tradeoffs, alternatives considered, follow-ups. -->
